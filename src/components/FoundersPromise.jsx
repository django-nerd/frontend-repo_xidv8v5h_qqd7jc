import React from 'react';

export default function FoundersPromise() {
  return (
    <section className="relative z-10 mx-auto max-w-5xl px-6 py-24 text-white">
      <div className="rounded-3xl border border-white/10 bg-white/[0.03] p-10 backdrop-blur">
        <h2 className="text-3xl font-bold">Founder’s Promise</h2>
        <p className="mt-4 text-white/70">
          We believe in practical, outcome-driven learning. Every module is built from real projects and tested playbooks so you can move fast, avoid noise, and build something that ships.
        </p>
        <p className="mt-4 text-white/70">
          Start free with Level 0, then join AI Launchpad System when you’re ready to go from idea to launch.
        </p>
      </div>
    </section>
  );
}
